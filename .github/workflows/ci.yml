name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  lint:
    name: ShellCheck & Syntax
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run ShellCheck
        run: |
          shellcheck install.sh ly/setup.sh ly/startup.sh limine/setup.sh tests/test_suite.sh

  test:
    name: Test Suite
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies (luajit)
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq luajit

      - name: Run test suite
        run: |
          chmod +x tests/test_suite.sh install.sh ly/setup.sh ly/startup.sh limine/setup.sh
          ./tests/test_suite.sh
